import { Direction, IconSize } from "../types";
export declare const useIconParams: (inputSize: IconSize, direction: Direction) => {
    size: number;
    strokeWidth: number;
    style: {
        transform: string;
    };
};
