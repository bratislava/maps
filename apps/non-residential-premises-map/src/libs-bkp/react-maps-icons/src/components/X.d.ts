/// <reference types="react" />
import { IIconComponentProps } from "../types";
export interface IXProps extends IIconComponentProps {
    className?: string;
}
export declare const X: ({ size: inputSize, direction, className, }: IXProps) => JSX.Element;
