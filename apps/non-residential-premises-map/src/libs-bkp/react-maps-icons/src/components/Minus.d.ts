/// <reference types="react" />
import { IIconComponentProps } from "../types";
export interface IMinusProps extends IIconComponentProps {
    className?: string;
}
export declare const Minus: ({ size: inputSize, direction, className, }: IMinusProps) => JSX.Element;
